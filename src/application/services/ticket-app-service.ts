import { TicketDomainService } from "../../domain/domain-services/ticket-domain-service";

export class TicketAppService {
  /*
  private readonly ITicketRepository tickets;
  private readonly ICheckListRepository checkLists;
  private readonly TicketDomainService ticketDomainService;

  constructor(tickets: ITicketRepository,
    checkLists: ICheckListRepository,
    ticketDomainService: TicketDomainService) {
    this.tickets = tickets;
    this.checkLists = checkLists;
    this.ticketDomainService = ticketDomainService;
  }

  static async changeStatus(): Promise<void> {
    await TicketDomainService.changeStatus(ticketId, status);
    return Promise.resolve();
  }
    */
}
